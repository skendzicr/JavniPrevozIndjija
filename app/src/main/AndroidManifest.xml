<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="solutions.graviton.jpi" >
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.internet" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Jpi" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PrigradskiActivity"
            android:screenOrientation="portrait" android:label="@string/prigradski_name">

         </activity>
        <activity
            android:name=".PozivActivity"
            android:screenOrientation="portrait"
            android:label="@string/poziv_name">

        </activity>
        <activity
            android:name=".GradskiActivity"
            android:screenOrientation="portrait"
            android:label="@string/gradski_name">
        </activity>
    </application>

</manifest>
